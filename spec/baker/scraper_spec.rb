require "spec_helper"

RSpec.describe Baker::Scraper do
  it "has a version number" do
    expect(Baker::Scraper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
